# Backend Configuration

# Podman Host (e.g., unix:///run/podman/podman.sock, unix:///run/user/1000/podman/podman.sock, tcp://localhost:8080)
PODMAN_HOST=

# Podman Connection Name (alternative to PODMAN_HOST, uses named connection from 'podman system connection list')
PODMAN_CONNECTION=

# JWT Secret Key (REQUIRED - change in production)
SECRET_KEY=your-secret-key-change-this-in-production

# Database URL (optional, defaults to sqlite:///./podsec.db)
DATABASE_URL=sqlite:///./podsec.db

# Server Configuration
PYTHONUNBUFFERED=1
